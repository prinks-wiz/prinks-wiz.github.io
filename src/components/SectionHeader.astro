---
interface Props {
  number: string; // e.g. "01"
  title: string;  // e.g. "WORK"
}
const { number, title } = Astro.props;
---
<div class="border-t border-edge pt-4 mb-10">
  <span class="font-mono text-xs text-meta font-medium tracking-[0.15em] select-none">
    § {number} / {title}
  </span>
</div>

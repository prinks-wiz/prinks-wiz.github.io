---
interface Props {
  org:         string;
  role:        string;
  startDate:   Date;
  endDate?:    Date | null;
  description: string;
  techStack?:  string[];
  location?:   string;
}
const { org, role, startDate, endDate, description, techStack, location } = Astro.props;

const fmt = (d: Date) =>
  d.toLocaleDateString('en-US', { year: 'numeric', month: 'short' });

const dateRange = `${fmt(startDate)} to ${endDate ? fmt(endDate) : 'Present'}`;
---
<article class="rounded-sm border border-edge bg-surface-subtle p-6 md:p-8 mb-6 transition-colors hover:border-accent">
  <div class="flex items-start justify-between gap-4 mb-4">
    <div class="flex items-start gap-4">
      <!-- Logo placeholder: swap with <img> when real logos are ready -->
      <div
        class="w-10 h-10 rounded-md shrink-0 mt-0.5"
        style="background-color: color-mix(in srgb, var(--accent) 10%, transparent);"
        aria-hidden="true"
      ></div>
      <div>
        <h3 class="text-2xl font-bold text-content">{role}</h3>
        <div class="flex flex-wrap items-center gap-x-2 gap-y-1 mt-1">
          <span class="text-base font-semibold text-secondary">{org}</span>
          {location && (
            <>
              <span class="text-secondary" aria-hidden="true">·</span>
              <span class="font-mono text-xs font-semibold text-secondary">{location}</span>
            </>
          )}
        </div>
      </div>
    </div>
    <time class="font-mono text-xs font-semibold text-secondary shrink-0 mt-1 text-right">
      {dateRange}
    </time>
  </div>

  <p class="text-base text-secondary leading-relaxed mb-5">{description}</p>

  {techStack && techStack.length > 0 && (
    <ul class="flex flex-wrap gap-2 list-none m-0 p-0" aria-label="Tech stack">
      {techStack.map(tech => (
        <li>
          <span class="font-mono text-xs font-semibold text-secondary bg-surface border border-edge px-2 py-0.5">
            {tech}
          </span>
        </li>
      ))}
    </ul>
  )}
</article>
